// module requires
const fs = require("fs");



let chartWacher = null;

// function for chart watcher
function chartWatcher(createNew = false, path) {
	console.log(path);
}


// function to handle all FileWatchers stuff
function setupWatchers(paths) {
	console.log(paths);
}

module.exports = { setupWatchers }
