// react imports
import React from "react";

// export the contexts
// default for file path stuff
export const PathFileContext = React.createContext([]);

// default for themes stuff
export const ThemeContext = React.createContext({});

// default for file data stuff
export const FileDataContext = React.createContext({});
